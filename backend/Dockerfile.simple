# 简化版Dockerfile用于测试
FROM node:18-alpine

# 基础工具
RUN apk add --no-cache curl

WORKDIR /app

# 复制并安装依赖
COPY package*.json ./
RUN npm install

# 复制源代码
COPY . .

# 编译代码
RUN npm run build

# 暴露端口
EXPOSE 3001

# 启动命令
CMD ["node", "dist/server.js"]